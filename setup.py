from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='review text summarization using encoder edcoder (bidirectional lstm) and attention model',
    author='sudil ravindran pk',
    license='',
)
